import 'package:flutter/cupertino.dart';

class Recurrings extends StatelessWidget {
  const Recurrings({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Recurrings');
  }
}
